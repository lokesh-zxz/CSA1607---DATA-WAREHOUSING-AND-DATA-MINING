name: 'adaptive_bi'
version: '1.0.0'
config-version: 2

profile: 'adaptive_bi'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  adaptive_bi:
    staging:
      +materialized: view
    marts:
      +materialized: table
